numpy>=1.21.0
pandas>=1.3.0
matplotlib>=3.4.0
jupyter>=1.0.0
seaborn>=0.11.0

# Dependencias opcionales para aceleración GPU
cupy-cuda13x>=13.0.0  # Para GPUs NVIDIA con CUDA 13.x (recomendado)
# cupy-cuda12x>=12.0.0  # Para GPUs NVIDIA con CUDA 12.x
# cupy-cuda11x>=11.0.0  # Para GPUs NVIDIA con CUDA 11.x

# Instalación de CuPy según versión de CUDA:
# pip install cupy-cuda13x  (para CUDA 13.x - RTX 40/50 series)
# pip install cupy-cuda12x  (para CUDA 12.x - RTX 30 series)
# pip install cupy-cuda11x  (para CUDA 11.x - RTX 20 series y anteriores)

# Verificar versión CUDA: nvidia-smi o nvcc --version